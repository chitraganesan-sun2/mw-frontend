<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+ System Splash Screen API attributes (via androidx.core:core-splashscreen
             compat, which also emulates this on API <31). android:background alone is the pre-
             core-splashscreen legacy approach and doesn't satisfy this theme's real requirements -
             the Capacitor SplashScreen plugin's installSplashScreen() call was silently failing
             and falling back to a path Google's own docs say doesn't work on Android 12+ at all,
             which is why the splash wasn't showing/holding for launchShowDuration on API 31+ devices. -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Kept for the pre-splashscreen-API legacy show() fallback path only. -->
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>